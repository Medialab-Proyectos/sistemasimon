/**
 * Design System SM — Main Entry Point (TypeScript)
 *
 * Import components:
 *   import { Button, Input, Badge, Checkbox, Avatar } from '@/lib/design-system';
 *
 * Import tokens:
 *   import { colors, spacing, radius } from '@/lib/design-system/tokens';
 */

export * from "./components";
export * from "./tokens";
